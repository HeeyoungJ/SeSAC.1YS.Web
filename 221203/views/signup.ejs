<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User page</title>
    <style>
    </style>
</head>
<body>
        <form action="form" id="user_form" align="center">
            <h3>가입하기</h3>
            ID.<br>
            <input type="text" id="id" name="id"><br>
            Password.<br>
            <input type="password" id="pw" name="pw"><br>
            Name.<br>
            <input type="text" id="name" name="name"></span>
            <br>
            <br>
            <button type="button" class="register" onclick="register();">가입하기</button>
        </form>
    <script>
        function register(){
                let form = document.getElementById("user_form");
                let data = {
                    id: form.id.value,
                    pw: form.pw.value,
                    name: form.name.value
                }
                axios({
                    method : 'post',
                    url : '/user/userlist',
                    data : FormData,
                    headers : {
                    'Content-Type': 'multipart/form-data'
                    }
                })
                .then((response)=>{
                    form.id.value = "";
                    form.name.value = "";
                    form.comment.value = "";
                })
                }
    </script>
</body>
</html>