<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h3>get 요청</h3>
    <form action="/getForm" method="get" name="getform">
    <!-- 서버로 기능을 전송할 수 있는 기능 제공 
    action : 폼(정보)을 전송할 주소(서버측에서 만든 주소)
    method : get(폼에 입력한 정보가 url에 추가됨) , post(보안상 더 좋다.) 둘 중 하나 들어갈 수 있음.
    name은 원하는 대로 입력 가능-->

        <input type="text" name="id">
        <input type="password" name="pw" pattern="^([a-zA-Z0-9가-힣]){8,}$">
    <!-- input 태그의 name은 필수 사항 -->

    <!-- 묶음 -->
    <fieldset>
        <legend>성별</legend>
    <!-- label : 글자 클릭시 체크 가능 / id : 부여 -->
        <input type="radio" name="gender" value="남자" id="men"><label for="men">남자</label>
        <input type="radio" name="gender" value="여자" id="women"><label for="women">여자</label>
    </fieldset>

    <fieldset>
        <legend>취미</legend>
    <!-- ***name으로 묶음 구분 / value값으로 서버에 알려주기 -->
        <input type="checkbox" name="interest" value="넷플릭스"> 넷플릭스
        <input type="checkbox" name="interest" value="음악감상"> 음악감상
        <input type="checkbox" name="interest" value="운동"> 운동
    </fieldset>

        <button type="submit">제출</button>
    </form>

    <h3>post 요청</h3>
    <form action="/postForm" method="post">
<!-- readonly : value값만 제출 할 수 있음. 수정 x / required : 필수 입력값 -->
        <input type="text" name="id" value="자동 부여" readonly required>
        <input type="password" name="pw" placeholder="비밀번호" required>
        
        <select name="birthMonth">
            <%for(var i = 1; i<=12; i++) {%>
                <option value="<%=i%>"><%=i%></option>
            <%}%>
        </select>

        <textarea name="content" rows="10" cols="30" required></textarea>
<!-- rows는 세로 글자 수, cols는 가로 글자 수 -->

        <button type="submit">제출</button>
    </form>

    <script>
        console.log(document.forms["getform"])
    </script>
</body>
</html>